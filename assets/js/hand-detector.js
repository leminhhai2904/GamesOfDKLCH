/**
 * Deprecated: hand-detector.js
 *
 * Hand detection was moved to a standalone Windows application
 * (`hand_detector_app.py`) that simulates local mouse clicks. The
 * in-browser detector and this file are no longer used by the web
 * frontend, which now only hosts the games.
 */

console.warn('hand-detector.js is deprecated and not used. Hand detection has moved to the standalone app.');
